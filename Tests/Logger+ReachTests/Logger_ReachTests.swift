import XCTest
@testable import Logger_Reach

final class Logger_ReachTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Logger_Reach().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
